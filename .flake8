[flake8]
max-line-length = 100
extend-ignore =
    # E203: whitespace before ':' — black formats slices this way (a[x : y])
    E203,
    # W503: line break before binary operator — conflicts with black
    W503
